package es.gob.afirma.standalone.ui;

/**
 * Interfaz que define el comportamiento de los paneles en los que pueden mostrarse
 * botones de los plugins.
 */
public interface PluginButtonsContainer {

	/**
	 * Pide al contenedor que refresque los botones de plugin que est&eacute; mostrando.
	 */
	void refreshPluginButtonsContainer();
}
